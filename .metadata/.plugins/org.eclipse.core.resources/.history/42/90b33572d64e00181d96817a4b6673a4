import doodlepad.*;

public class board {
	public static void main (String [] args) {
		Pad gamescreen = new Pad (500, 700);
		int turnNum = 1;
		setupGame();
		
	}

	private static void setupGame() {

		for (int i = 0; i > 8; i++) {
            Rectangle box = new Rectangle(i*80, i*80, 80, 80);
		}
		
	}
}
