import doodlepad.*;

public class board {
	public static void main (String [] args) {
		Pad gamescreen = new Pad (750, 1000);
		int turnNum = 1;
		setupTurn(turnNum);
		
	}

	private static void setupTurn(int turnNum) {
        Rectangle pegBoard = new Rectangle(25, 500, 75, 75);
		
	}
}
